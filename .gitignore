# Dependencies, build output, and local runtime data (also covered per-app).
node_modules/
dist/
data/
*.db
*.db-shm
*.db-wal

# moon generated (must never be committed — stale cache breaks CI)
.moon/cache/
.moon/docker/

# image build artifacts (melange/apko)
packages/
*.tar
melange.rsa
melange.rsa.pub
sbom/
